<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kbstar.littlestar.quiz.mapper.QuestionMapper">
    <select id="getRandomQuestion" resultType="com.kbstar.littlestar.quiz.domain.Question">
        select id, question, option1, option2, option3, option4, answer, answer_index as answerIndex, explanation, created_at, updated_at
        from question
        order by rand()
        limit 1
    </select>
</mapper>